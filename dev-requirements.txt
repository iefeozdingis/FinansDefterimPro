pre-commit>=3.5,<5
# Üst sınır <2 idi: CI mypy 1.x kurarken geliştirme ortamında 2.x
# kullanılıyordu ve iki sürüm import çözümlemesinde farklı davrandığı için
# "yerelde geçiyor, CI'da patlıyor" durumu oluşuyordu.
mypy>=1.8,<3
black>=24.0,<27
isort>=5.12,<7
flake8>=6.1,<8
